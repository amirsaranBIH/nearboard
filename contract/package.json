{
  "name": "nearboard",
  "version": "1.0.0",
  "license": "(MIT AND Apache-2.0)",
  "type": "module",
  "scripts": {
    "build": "near-sdk-js build src/contract.ts build/nearboard.wasm",
    "deploy": "npm run build && near dev-deploy --wasmFile build/nearboard.wasm",
    "test": "echo use integration-tests"
  },
  "dependencies": {
    "near-cli": "^3.4.0",
    "near-sdk-js": "0.6.0-0"
  },
  "devDependencies": {
    "typescript": "^4.7.4"
  }
}
